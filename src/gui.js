function startGUI() {
  //speedSlider = createSlider(10, 1000, 40);
  //speedSlider.elt.id = 'firstSlider'
  //speedSlider.position(20, 700);

  /*
  const slider = document.createElement('input')
  slider.type = 'range'
  slider.min = 10
  slider.id = 'mySlider'
  const ui = document.querySelector('#ui')
  ui.appendChild(slider)
  */

}

function resizeGUI() {
  //if (windowWidth < 960) {
    //speedSlider.position(20, 600);
  //} else {
  	//speedSlider.position(540, 20);
  //}
  //console.log(windowWidth);
}